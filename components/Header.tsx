// Feste Kopfzeile mit Wortmarke — erscheint auf Start, Liste, Karte, Magazin.
// Reiner Text, kein Symbol, kein Bild, nie im Akzent.
export default function Header() {
  return (
    <header className="site-header">
      <span className="wordmark">KAVERNE</span>
    </header>
  );
}
